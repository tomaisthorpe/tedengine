# Mesh Component

import useBrowserOnly from '@hooks/useBrowserOnly';
import { TGame } from '@tedengine/ted';

export const Game = () => {
  const onBrowser = useBrowserOnly();
  if (!onBrowser) {
    return null;
  }
  return (
    <TGame
      aspectRatio="auto"
      game={
        new Worker(new URL('@examples/3d/mesh-component.ts', import.meta.url))
      }
    />
  );
};

<Game />
