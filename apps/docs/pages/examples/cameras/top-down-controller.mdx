# Top Down Controller

Use WASD to move the box.

import useBrowserOnly from '@hooks/useBrowserOnly';
import { TGame } from '@tedengine/ted';

export const Game = () => {
  const onBrowser = useBrowserOnly();
  if (!onBrowser) {
    return null;
  }
  return (
    <TGame
      game={new Worker(new URL('@examples/cameras/top-down-controller.ts', import.meta.url))}
    />
  );
};

<Game />
