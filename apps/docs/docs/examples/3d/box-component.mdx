# Box Component

import useIsBrowser from '@docusaurus/useIsBrowser';
import { TGame } from '@tedengine/ted';

export function Game() {
  const onBrowser = useIsBrowser();
  if (!onBrowser) {
    return null;
  }
  return (
    <TGame
      game={
        new Worker(new URL('@examples/3d/box-component.ts', import.meta.url))
      }
    />
  );
}

<Game />
