# Empty Project

This example shows the bare minimum code required for the engine to run.

import useBrowserOnly from '@docusaurus/useIsBrowser';
import { TGame } from '@tedengine/ted';

export const Game = () => {
  const onBrowser = useBrowserOnly();
  if (!onBrowser) {
    return null;
  }
  return (
    <TGame
      game={
        new Worker(new URL('@examples/empty-project.ts', import.meta.url))
      }
    />
  );
};

<Game />
