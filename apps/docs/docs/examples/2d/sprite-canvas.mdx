# Sprite Canvas

The standard 2D canvas context can be used to create textures which can then be used to create sprites.

import useBrowserOnly from '@docusaurus/useIsBrowser';
import { TGame } from '@tedengine/ted';

export const Game = () => {
  const onBrowser = useBrowserOnly();
  if (!onBrowser) {
    return null;
  }
  return (
    <TGame
      game={
        new Worker(new URL('@examples/2d/sprite-canvas.ts', import.meta.url))
      }
    />
  );
};

<Game />
